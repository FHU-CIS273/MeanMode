using System;

namespace MeanMode
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Hello World!");
        }



        /**
         * <summary> Determines if the mean and mode are the same value. </summary>
         * <returns>true if the mean = the mode
         * otherwise, false.</returns>
         * <remarks> If there is more than one mode, then return false. </remarks>
         */
        public static bool MeanMode(int[] array)
        {
            return true;
        }
    }
}
